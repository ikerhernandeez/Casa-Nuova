<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Página Moderna</title>
    <link rel="stylesheet" href="estilos.css">
</head>
<body>
    <!-- Encabezado -->
    <header>
        <h1>Mi Página Moderna</h1>
        <nav>
            <a href="#inicio">Inicio</a>
            <a href="#sobre-mi">Sobre mí</a>
            <a href="#portfolio">Portfolio</a>
            <a href="#contacto">Contacto</a>
        </nav>
    </header>

    <!-- Sección de inicio -->
    <section id="inicio" class="hero">
        <h2>Bienvenido a mi sitio web</h2>
        <p>Descubre mi mundo creativo y proyectos interesantes.</p>
        <a href="#portfolio" class="btn">Ver mis proyectos</a>
    </section>

    <!-- Sección sobre mí -->
    <section id="sobre-mi">
        <h2>Sobre mí</h2>
        <p>Soy un apasionado del diseño web y la tecnología. Me encanta crear páginas modernas y funcionales.</p>
    </section>

    <!-- Sección portfolio -->
    <section id="portfolio">
        <h2>Portfolio</h2>
        <div class="grid">
            <div class="card">Proyecto 1</div>
            <div class="card">Proyecto 2</div>
            <div class="card">Proyecto 3</div>
            <div class="card">Proyecto 4</div>
        </div>
    </section>

    <!-- Sección contacto -->
    <section id="contacto">
        <h2>Contacto</h2>
        <form>
            <input type="text" placeholder="Nombre" required>
            <input type="email" placeholder="Correo" required>
            <textarea placeholder="Mensaje" required></textarea>
            <button type="submit">Enviar</button>
        </form>
    </section>

    <!-- Pie de página -->
    <footer>
        <p>© 2026 Mi Página Moderna</p>
    </footer>
</body>
</html>
/* Reseteo básico */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    line-height: 1.6;
    color: #333;
    background-color: #f0f2f5;
}

/* Encabezado */
header {
    background-color: #1e3a8a;
    color: white;
    padding: 1em 0;
    text-align: center;
}

header nav a {
    color: white;
    margin: 0 1em;
    text-decoration: none;
    font-weight: bold;
}

header nav a:hover {
    text-decoration: underline;
}

/* Hero */
.hero {
    background: linear-gradient(to right, #3b82f6, #60a5fa);
    color: white;
    text-align: center;
    padding: 4em 1em;
}

.hero .btn {
    display: inline-block;
    padding: 0.75em 1.5em;
    background-color: #1e40af;
    color: white;
    border-radius: 5px;
    text-decoration: none;
    margin-top: 1em;
}

.hero .btn:hover {
    background-color: #1e3a8a;
}

/* Secciones */
section {
    padding: 3em 1em;
    max-width: 900px;
    margin: 0 auto 2em auto;
    background-color: white;
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
}

/* Portfolio */
.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1em;
    margin-top: 1em;
}

.card {
    background-color: #3b82f6;
    color: white;
    padding: 2em;
    border-radius: 8px;
    text-align: center;
    font-weight: bold;
}

/* Contacto */
form {
    display: flex;
    flex-direction: column;
    gap: 1em;
}

input, textarea {
    padding: 0.75em;
    border-radius: 5px;
    border: 1px solid #ccc;
    width: 100%;
}

button {
    padding: 0.75em;
    background-color: #3b82f6;
    color: white;
    border: none;
    border-radius: 5px;
    font-weight: bold;
    cursor: pointer;
}

button:hover {
    background-color: #1e40af;
}

/* Pie de página */
footer {
    text-align: center;
    padding: 1em 0;
    background-color: #1e3a8a;
    color: white;
}
